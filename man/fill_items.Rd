% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fill.R
\name{fill_items}
\alias{fill_items}
\title{Interactively fill in the reporting items at the R console}
\usage{
fill_items(
  items = NULL,
  study_type = c("both", "lab", "field"),
  save_to = NULL
)
}
\arguments{
\item{items}{A data frame of items to start from. If \code{NULL}, starts from
a blank \code{\link[=framework_template]{framework_template()}}. Pass an existing fill in to resume.}

\item{study_type}{Passed to \code{\link[=framework_template]{framework_template()}} if \code{items} is \code{NULL}.}

\item{save_to}{Optional path to save to on \code{save} (without an argument)
and on normal exit.}
}
\value{
The (possibly partially) filled items data frame, invisibly.
}
\description{
Walks through the 18 framework items in canonical order, prompting for a
value for each. Designed for use at the R or RStudio console. You can
stop at any point with \code{quit}, save with \verb{save path/to/file.csv}, and
resume later by passing the saved file back in (via \code{\link[=load_items]{load_items()}}) or
the returned object directly.
}
\details{
Each prompt shows the item's domain, name, and description so you do
not need to remember \code{item_id}s or indexing.
}
\examples{
\dontrun{
# Start fresh
items <- fill_items()

# Save partway and resume later
items <- fill_items(save_to = "my_study.csv")
# ...later...
items <- fill_items(load_items("my_study.csv"), save_to = "my_study.csv")
}

}
