% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/framework.R
\name{framework_template}
\alias{framework_template}
\title{Return an empty items template for a study}
\usage{
framework_template(study_type = c("both", "lab", "field"))
}
\arguments{
\item{study_type}{Optional. One of \code{"lab"}, \code{"field"}, or \code{"both"}.
If supplied, items flagged as not applicable in that context have
their \code{value} pre-set to \code{"NA"} as a default.}
}
\value{
A data frame with columns \code{item_id}, \code{item}, \code{domain},
\code{description}, \code{value}.
}
\description{
Convenience helper that returns a data frame with one row per framework
item and an empty \code{value} column for the user to fill in.
}
\details{
Leave \code{value} as \code{""} for items not reported. Set to \code{"NA"} (the
character string) for items that are not applicable to the study.
}
\examples{
tmpl <- framework_template("lab")
head(tmpl)

}
