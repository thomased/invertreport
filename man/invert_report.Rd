% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/invert_report.R
\name{invert_report}
\alias{invert_report}
\title{Build an invertebrate welfare reporting figure}
\usage{
invert_report(paper, items, value_wrap = 75, strict = TRUE)
}
\arguments{
\item{paper}{A named list of paper metadata. Required: \code{title},
\code{authors}. Optional: \code{journal}, \code{version}, \code{doi}.}

\item{items}{A data frame with at least \code{item_id} and \code{value} columns.
Items with \code{value = ""} (or \code{NA}) render as "Not reported". Items with
\code{value = "NA"} (the character string) render as "Not applicable".
Use \code{\link[=framework_template]{framework_template()}} to obtain a ready-to-fill template.}

\item{value_wrap}{Integer; approximate characters per line for the value
text. Defaults to \code{75}, which fills the card width at the default
save dimensions; reduce for narrower outputs.}

\item{strict}{Logical; if \code{TRUE}, unknown \code{item_id}s in \code{items} raise an
error. If \code{FALSE}, they are warned about and ignored. Defaults to \code{TRUE}.}
}
\value{
An object of class \code{invert_report} (a patchwork composition with
added metadata). Can be \code{print()}ed, \code{plot()}ted, or saved with
\code{\link[=save_report]{save_report()}} or \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.
}
\description{
Produces a standardised, publication-ready summary figure of invertebrate
welfare reporting for a given paper, in the spirit of a PRISMA flow
diagram: each cell of the figure carries the substantive content for the
corresponding item.
}
\examples{
tmpl <- framework_template()
tmpl$value[tmpl$item_id == "subjects_taxon"] <-
  "Bombus terrestris (worker female); morphology + COI"
tmpl$value[tmpl$item_id == "proc_anaesthesia"] <- "NA"

fig <- invert_report(
  paper = list(title = "Demo", authors = "Smith et al. (2026)"),
  items = tmpl
)
\dontrun{ plot(fig) }

}
