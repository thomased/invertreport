% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/invert_report.R
\name{save_report}
\alias{save_report}
\title{Save a report figure to disk}
\usage{
save_report(report, filename, width = 8.5, height = NULL, dpi = 300, ...)
}
\arguments{
\item{report}{An object of class \code{invert_report}.}

\item{filename}{Output file path (extension determines format; .pdf or
.png are recommended).}

\item{width}{Page width in inches. Defaults to 8.5".}

\item{height}{Page height in inches. If \code{NULL} (the default), a
compact height is chosen from the content; the minimum is \verb{6"} and
the maximum is \verb{11"} (US Letter).}

\item{dpi}{Resolution for raster formats. Defaults to 300.}

\item{...}{Additional arguments passed to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}
}
\value{
The filename, invisibly.
}
\description{
Thin wrapper around \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} with sensible defaults for
the figure produced by \code{\link[=invert_report]{invert_report()}}. The default height is
computed from the figure's natural content size so the saved file is
as compact as the content allows, with no large blank regions. Pass
an explicit \code{height} to override.
}
\examples{
\dontrun{
tmpl <- framework_template()
fig  <- invert_report(list(title = "T", authors = "A"), tmpl)
save_report(fig, "welfare_reporting.pdf")
}

}
