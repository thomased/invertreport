# Intentionally empty in the bundled Shiny app copy.
#
# The package-level documentation lives in the installed package at
# R/invertreport-package.R. It is not needed inside the Shiny app
# itself, and including it would put the package name in shinylive's
# scanning surface.
